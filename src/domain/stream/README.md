# Stream Domain Layer

Purpose:
- Define stream domain models, value objects, and pure business rules.

Rules:
- Keep domain code side-effect free where practical.
- Avoid infrastructure/network dependencies.
- Expose reusable decisions used by services.
