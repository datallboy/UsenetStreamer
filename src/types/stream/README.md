# Stream Types

Purpose:
- Centralize stream contracts, payload shapes, and template context types.

Current files:
- `payload.ts`: stream response payload structures.
- `contracts.ts`: stream ranking and bucket contracts.
- `template-context.ts`: naming/template rendering context.

Guidance:
- Add new stream-facing DTOs here before wiring logic changes.
